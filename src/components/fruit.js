import React from 'react';

const Fruit = () => (
    <div style={{ textAlign: 'center', fontSize: '5rem' }}>
        🍎
        <div style={{ fontSize: '1rem', marginTop: '0.5rem' }}>Apple</div>
    </div>
);

export default Fruit;